import {is} from 'xpress-mongo';
import {DBCollection} from '@xpresser/xpress-mongo';

/**
* Interface for Model's `this.data`.
* Optional if accessing data using model helper functions
*/
export interface {{name}}DataType {
    updatedAt?: Date,
    createdAt: Date,
}

/**
 * Model {{name}}
 */
class {{name}} extends DBCollection('{{table}}') {

    // Set Model Schema
    static schema = {
        updatedAt: is.Date(),
        createdAt: is.Date().required()
    };

    public data!: {{name}}DataType;
}

export default {{name}};

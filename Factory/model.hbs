const {is} = require("xpress-mongo");
const {DBCollection} = require("@xpresser/xpress-mongo/util");

// Model Schema
const {{name}}Schema = {
    updatedAt: is.Date(),
    createdAt: is.Date().required()
};

/**
* {{name}} Model Class
*/
class {{name}} extends DBCollection("{{table}}") {

    constructor(){
        super();
        // Register Schema
        this.useSchema({{name}}Schema);
    }

}

module.exports = {{name}}

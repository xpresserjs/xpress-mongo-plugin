const {is, RefreshDateOnUpdate} = require("xpress-mongo");
const {DBCollection} = require("@xpresser/xpress-mongo/util");

/**
* {{name}} Model Class
*/
class {{name}} extends DBCollection("{{table}}") {

    // Set Model Schema
    static schema = {
        updatedAt: is.Date(),
        createdAt: is.Date().required()
    };

}

/**
* Adds event to set `updatedAt` Timestamp to current date on update.
* Remove if not in use.
*/
RefreshDateOnUpdate({{name}}, 'updatedAt');


// Export Model
module.exports = {{name}}
